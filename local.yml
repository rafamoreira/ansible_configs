---
- hosts: localhost
  connection: local
  become: true

  tasks:
    - name: install base packages
      package:
        name:
          - htop
          - tmux
          - vim
          - keychain
          - openssh
          - wget
          - pyhton
